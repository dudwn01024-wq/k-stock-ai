# K-Stock AI Codex 작업 규칙

## 1. 기본 작업 원칙

- 작업 전에 관련 코드를 먼저 분석한다.
- 사용자가 요청한 기능과 직접 관련된 부분만 수정한다.
- 정상 작동 중인 기존 기능을 임의로 삭제하거나 변경하지 않는다.
- 불필요한 대규모 리팩터링이나 구조 변경을 하지 않는다.
- 수정 범위를 최소화한다.
- 확실하지 않은 부분은 추측해서 구현하지 않는다.

## 2. 실제 금융 데이터 규칙

- 주가, 거래량, 거래대금, 외국인 수급, 기관 수급 등 금융 데이터를 임의 생성하거나 추정하지 않는다.
- 실제 API 또는 실제 데이터 소스에서 얻은 값만 사용한다.
- 데이터를 얻지 못하면 가짜 숫자를 만들지 말고 null, UNKNOWN 또는 데이터 없음 상태로 처리한다.
- 테스트용 mock 데이터는 운영 데이터와 명확히 분리한다.
- 실제 데이터와 AI가 생성한 분석 내용을 구분한다.

## 3. 분석 및 추천 규칙

- AI가 실제 시장 데이터를 임의로 만들어서는 안 된다.
- 종목 추천은 실제 수집된 데이터만 근거로 사용한다.
- 추세, 거래량, 수급, 뉴스, 리스크 조건을 구분해서 판단한다.
- 데이터가 없는 UNKNOWN 상태와 조건 불충족 FAIL 상태를 동일하게 처리하지 않는다.
- AI 추천 결과가 실제 매수 주문으로 바로 연결되어서는 안 된다.

## 4. 진입가 / 익절가 / 손절가

- 진입가, 목표가, 손절가를 AI가 근거 없이 임의 생성하지 않는다.
- 기존 계산 방식이 있는 경우 먼저 분석한다.
- 계산 로직 변경 전 기존 방식과 영향을 확인한다.
- 실제 수집된 가격, 차트, 거래량, 수급 등 검증된 데이터를 사용한다.

## 5. 자동매매 안전 규칙

- 실제 주문 실행 로직은 사용자의 명시적인 요청 없이 변경하지 않는다.
- 실제 매수/매도 API를 테스트 과정에서 호출하지 않는다.
- 매수/매도 조건을 임의로 완화하거나 제거하지 않는다.
- 손절, 최대 투자금액, 주문 수량, 최대 보유종목 수 등 위험관리 조건을 임의로 변경하지 않는다.
- 동일 종목 중복 매수와 동일 주문 중복 실행을 방지한다.
- 모의매매와 실거래를 명확히 분리한다.
- PAPER\_MODE 또는 이에 준하는 안전장치가 있다면 유지한다.
- 데이터가 불확실하거나 핵심 데이터가 누락되면 거래하지 않는 방향으로 처리한다.

## 6. Risk Manager 원칙

- Risk Manager는 AI 추천보다 우선한다.
- AI가 강한 매수 의견을 내더라도 위험조건을 통과하지 못하면 주문을 실행하지 않는다.
- 일일 최대 손실, 종목당 최대 투자금, 최대 보유종목 수, 연속 손실 제한, 손절 조건 등 안전장치를 임의로 제거하지 않는다.

## 7. 주문 시스템 원칙

- 주문 요청 전에 현재 주문/보유 상태를 확인한다.
- 네트워크 오류나 API 응답 지연 때문에 동일 주문이 반복 실행되지 않도록 한다.
- 주문 성공 여부가 불확실할 때 재주문부터 하지 말고 주문 상태를 먼저 확인한다.
- 시장 휴장, 장 종료, API 오류 등 비정상 상황에서 잘못된 주문을 발생시키지 않는다.

## 8. API 및 보안

- API KEY, SECRET KEY, 비밀번호, 토큰을 소스코드에 직접 작성하지 않는다.
- 민감정보는 환경변수(.env, Render Environment Variables 등)를 사용한다.
- .env와 비밀키를 GitHub에 커밋하지 않는다.
- 로그에 API 키나 개인정보를 출력하지 않는다.
- 기존 보안 설정을 임의로 약화하지 않는다.

## 9. 코드 수정 절차

코드 수정 시 다음 순서를 따른다.

1. 관련 파일을 먼저 분석한다.
2. 현재 동작 방식을 파악한다.
3. 문제 원인을 확인한다.
4. 필요한 최소 범위만 수정한다.
5. 빌드 및 가능한 테스트를 실행한다.
6. 오류가 있으면 원인을 확인하고 수정한다.
7. 기존 기능에 영향이 없는지 검토한다.
8. 변경사항을 다시 검토한다.

## 10. Git 규칙

- main 브랜치에 큰 변경을 직접 적용하지 않는 것을 기본 원칙으로 한다.
- 중요한 작업은 별도 브랜치에서 수행한다.
- 사용자가 요청하지 않으면 자동으로 commit, push, merge 하지 않는다.
- 테스트가 실패한 상태에서는 커밋하지 않는다.
- force push를 사용하지 않는다.
- 기존 Git 기록을 임의로 삭제하거나 변경하지 않는다.

## 11. Render 배포 규칙

- 사용자의 명시적인 요청 없이 운영 환경에 배포하지 않는다.
- 배포 전에 빌드 및 테스트 상태를 확인한다.
- 배포 실패 시 추가 변경을 반복하기 전에 실패 원인을 먼저 분석한다.
- Render 환경변수를 코드에 복사하거나 노출하지 않는다.

## 12. 작업 범위 관리

- 하나의 요청 안에 서로 다른 대규모 기능이 섞여 있으면 작업 범위를 먼저 나눈다.
- 자동매매 핵심 로직, Risk Manager, 주문 시스템, 보안, 데이터베이스 구조 변경은 서로 독립적인 작업으로 취급한다.
- 요청 범위를 벗어난 문제를 발견하면 임의로 수정하지 말고 먼저 보고한다.

## 13. 작업 완료 보고

작업이 끝나면 반드시 다음 내용을 간단히 보고한다.

- 무엇을 변경했는지
- 어떤 파일을 변경했는지
- 변경 이유
- 실행한 테스트
- 테스트 결과
- 남아 있는 위험 또는 확인할 사항
- Git 커밋 여부
- GitHub push 여부
- Render 배포 여부

## 14. 최우선 원칙

- 실제 돈이 관련된 자동매매 시스템이므로 안전성을 편의성보다 우선한다.
- 잘못된 주문을 실행하는 것보다 주문을 실행하지 않는 것을 우선한다.
- 금융 데이터가 불확실할 경우 임의로 보완하거나 추측하지 않는다.
